diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x Makefile.in -x makefile.in -x configure -x '*.stamp' -x '*.html' -x '*.bak' -x '*.sgml' -x '*.tmpl' -x '*.txt' -x '*.xml' -x '*.args' -x '*.devhelp*' -x '*.css' -x '*.hierarchy' -x '*.interfaces' -x '*.prerequisites' -x '*.signals' -x '*.png' -x '*.top' -x '*.bottom' -x gtk-doc.make -x gtk-doc.m4 -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/exo-0.3.2/exo/exo-mount-point.c src/exo-0.3.2/exo/exo-mount-point.c
--- origsrc/exo-0.3.2/exo/exo-mount-point.c	2007-01-20 07:58:22.000000000 -0600
+++ src/exo-0.3.2/exo/exo-mount-point.c	2007-05-10 23:17:53.031250000 -0500
@@ -193,7 +193,9 @@
 {
   GSList *mount_points = NULL;
 
-#if defined(HAVE_SETMNTENT) /* Linux */
+#if defined(__CYGWIN__)
+  return NULL;
+#elif defined(HAVE_SETMNTENT) /* Linux */
   struct mntent *mntent;
   FILE          *fp;
 
@@ -295,7 +297,9 @@
 {
   GSList *mount_points = NULL;
 
-#if defined(HAVE_SETMNTENT) /* Linux */
+#if defined(__CYGWIN__)
+  return NULL;
+#elif defined(HAVE_SETMNTENT) /* Linux */
   struct mntent *mntent;
   FILE          *fp;
 
diff -urN -x CYGWIN-PATCHES -x 'aclocal.m4*' -x autom4te.cache -x config.cache -x config.log -x config.status -x config.h -x config.h.in -x ABOUT-NLS -x Makefile.in.in -x Makevars.template -x '*SlackBuild*' -x '*.class' -x '*.pyc' -x '*.mo' -x '*.gmo' -x '*.orig' -x '*.rej' -x '*.spec' -x '*.temp' -x '*~' -x '*.stackdump' -x COPYING -x INSTALL -x compile -x config-ml.in -x config.guess -x config.sub -x depcomp -x elisp-comp -x install-sh -x ltmain.sh -x mdate-sh -x missing -x mkinstalldirs -x py-compile -x symlink-tree -x texinfo.tex -x ylwrap -x Makefile.in -x makefile.in -x configure -x '*.stamp' -x '*.html' -x '*.bak' -x '*.sgml' -x '*.tmpl' -x '*.txt' -x '*.xml' -x '*.args' -x '*.devhelp*' -x '*.css' -x '*.hierarchy' -x '*.interfaces' -x '*.prerequisites' -x '*.signals' -x '*.png' -x '*.top' -x '*.bottom' -x gtk-doc.make -x gtk-doc.m4 -x omf.make -x xmldocs.make -x gnome-doc-utils.make -x gnome-doc-utils.m4 -x intltool.m4 -x intltool-extract -x intltool-extract.in -x intltool-merge -x intltool-merge.in -x intltool-update -x intltool-update.in origsrc/exo-0.3.2/python/Makefile.am src/exo-0.3.2/python/Makefile.am
--- origsrc/exo-0.3.2/python/Makefile.am	2007-01-20 07:58:18.000000000 -0600
+++ src/exo-0.3.2/python/Makefile.am	2007-05-06 21:48:21.437500000 -0500
@@ -31,7 +31,8 @@
 _exo_la_LIBADD =							\
 	$(top_builddir)/exo/libexo-$(LIBEXO_VERSION_API).la		\
 	$(GTK_LIBS)							\
-	$(LIBXFCE4UTIL_LIBS)
+	$(LIBXFCE4UTIL_LIBS)				\
+	$(PYTHON_LIBS)
 
 _exo_la_SOURCES =							\
 	exomodule.c							\
